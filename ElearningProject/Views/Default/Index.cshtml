
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

@Html.Action("PartialHead","Default")

<body>
    <!-- Spinner Start -->
    @Html.Action("PartialSpinner","Default")
    <!-- Spinner End -->
    <!-- Navbar Start -->
    @Html.Action("PartialNavbar","Default")
    <!-- Navbar End -->
    <!-- Carousel Start -->
    @Html.Action("PartialCarousel","Default")
    <!-- Carousel End -->
    <!-- Service Start -->
    @Html.Action("PartialServices","Default")
    <!-- Service End -->
    <!-- About Start -->
    @Html.Action("PartialAbout","Default")
    <!-- About End -->
    <!-- Categories Start -->
    @Html.Action("PartialCategory","Default")
    <!-- Categories Start -->
    <!-- Courses Start -->
    @Html.Action("PartialCourse","Default")
    <!-- Courses End -->
    <!-- Team Start -->
    @Html.Action("PartialTeam","Default")
    <!-- Team End -->
    <!-- Testimonial Start -->
    @Html.Action("PartialTestimonial","Default")
    <!-- Testimonial End -->
    <!-- Footer Start -->
    @Html.Action("PartialFooter","Default")
    <!-- Footer End -->
    <!-- Back to Top -->
    <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    @Html.Action("PartialScript","Default")
</body>

</html>